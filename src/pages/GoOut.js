import React from 'react';

const GoOut = () => (
  <div className="container">
    <h2>Go Out Page</h2>
    {/* Add components for setting preferences and viewing event listings */}
  </div>
);

export default GoOut;
